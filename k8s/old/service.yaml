apiVersion: v1
kind: Service
metadata:
  name: balance-old-service
spec:
  type: ClusterIP
  selector:
    app: balance-old
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 8080
